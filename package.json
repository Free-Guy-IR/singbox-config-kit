{
  "name": "@pasarguard/singbox-config-kit",
  "version": "0.1.0",
  "description": "Browser-safe TypeScript helpers for generating and validating sing-box (Hysteria2) core configuration JSON.",
  "type": "module",
  "packageManager": "bun",
  "license": "AGPL-3.0",
  "author": "PasarGuard",
  "repository": "https://github.com/PasarGuard/singbox-config-kit",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prepare": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.5.4"
  }
}
